import{readFile,writeFile}from'node:fs/promises';for(const file of ['README.md','README_ja.md','docs/tutorial.md','docs/tutorial_ja.md']){let s=await readFile(file,'utf8');s=s.replaceAll('https://cdn.jsdelivr.net/npm/@hachiware-labs/finch-js@0.5.1/dist/finch.global.js','../dist/finch.global.js');s=s.replace('Save the following as `diagram.html` and open it in a browser. The example uses the version-pinned browser build, so no installation or build step is required.','From the repository root, run `npm ci` and `npm run build`. Save the following as `examples/diagram.html`, run `python -m http.server 8000`, and open `http://127.0.0.1:8000/examples/diagram.html`. This uses the current checkout.');s=s.replace('次の内容を `diagram.html` として保存し、ブラウザーで開いてください。バージョンを固定したブラウザー版を読み込むため、インストールもビルドも必要ありません。','リポジトリのルートで `npm ci` と `npm run build` を実行します。次の内容を `examples/diagram.html` に保存し、`python -m http.server 8000` を起動して `http://127.0.0.1:8000/examples/diagram.html` を開いてください。現在のチェックアウトを使います。');s=s.replace('Save this page as `order-flow.html`, then open it in a browser. It uses a version-pinned CDN build, so it works outside the Finch.js repository without an install step.','From the repository root, run `npm ci` and `npm run build`. Save this page as `examples/order-flow.html`, run `python -m http.server 8000`, and open `http://127.0.0.1:8000/examples/order-flow.html`. The examples and images use the current checkout.');s=s.replace('次のページを `order-flow.html` として保存し、ブラウザーで開いてください。バージョンを固定した CDN 版を使うため、Finch.js のリポジトリ外でもインストールなしで動きます。','リポジトリのルートで `npm ci` と `npm run build` を実行します。次のページを `examples/order-flow.html` に保存し、`python -m http.server 8000` を起動して `http://127.0.0.1:8000/examples/order-flow.html` を開いてください。コードと画像は現在のチェックアウトを使います。');s=s.replace('The image and editable page use the current repository build; layout and editor UI may differ from the pinned CDN version.','The image and editable page use the same repository build as the example above.');s=s.replace('上の画像と編集用ページは現在のリポジトリビルドで描画しています。固定CDN版とは配置や編集UIが異なる場合があります。','上の画像と編集用ページは、掲載コードと同じリポジトリビルドで描画しています。');s=s.replaceAll('上の固定CDN版0.5.1ではなく、','').replace('The pinned 0.5.1 CDN build in section 1 does not include these additions. ','');await writeFile(file,s);}
